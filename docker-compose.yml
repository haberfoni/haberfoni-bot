version: '3.8'

services:
  bot:
    build: .
    container_name: haberfoni_bot
    restart: always
    environment:
      DATABASE_URL: ${DATABASE_URL}
    networks:
      - app_network

networks:
  app_network:
    external: true
